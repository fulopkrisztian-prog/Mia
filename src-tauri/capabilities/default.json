{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "floater"],
  "permissions": [
    "core:default",
    "opener:default",
    "opener:allow-default-urls",
    "dialog:default",
    "dialog:allow-open"
  ]
}